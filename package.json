{
  "name": "nodejs-api-mcfly",
  "version": "1.0.0",
  "description": "Exercise to create a simple backend based on a REST API with Node.js and Express",
  "main": "src/index.js",
  "scripts": {
    "start": "node --require 'babel-register' src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Víctor del Val",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-register": "^6.26.0"
  },
  "dependencies": {
    "express": "^4.16.3"
  }
}
